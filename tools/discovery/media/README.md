Demo recordings (`demo-start-cloudshell.gif`, `demo-download-output.gif`) will
land here when the upstream `ElisaPiccin/azure-ai-deployment-scanner` repo is
folded in via `git subtree`. See `../README.md` for instructions.
